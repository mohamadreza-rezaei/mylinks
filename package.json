{
  "name": "mylinks",
  "version": "1.0.0",
  "description": "Centralized redirect worker for all social links",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}